今天学习了 HashMap 的基本使用，包括 put、get 和三种遍历方式。
理解了 HashMap 的 put 流程：先根据 key 计算 hash，再定位数组下标，
如果桶为空则直接插入，如果发生冲突则挂到链表或红黑树上。
同时学习了 HashMap 的扩容机制，默认容量为16，加载因子为0.75，
超过阈值后会扩容为原来的2倍。
还掌握了 JDK8 中链表转红黑树的条件：链表长度达到8且数组长度至少为64。
最后对比了 JDK7 和 JDK8 在底层结构和插入方式上的区别。