# PreparedStatement 预编译总结

## 1. 什么是 PreparedStatement
    PreparedStatement 是 JDBC 中用于执行 SQL 的接口，支持预编译和参数化查询。

## 2. 什么是预编译
    预编译是指：
        SQL 语句结构提前编译
        参数在执行时传入

    例如：
        SELECT * FROM student WHERE name = ?